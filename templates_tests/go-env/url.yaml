options:
  go_meta: |
    // is URL? {{ isURL getTemplatePath }}

groups:
  - name: App
    description: Basic application settings
    fields:
      - name: Debug
        type: bool
        description: Enable debug mode
        default: "false"
      - name: Port
        type: int
        description: Server port
        required: true 